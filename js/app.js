window.onload=function(){
document.getElementById("button").onclick=runButton;

let a="";
let b="";
let c="";
let d="";
let e="";
let g="";

const runButton= function(){

      var todo = prompt("Enter an item to add to your list.");
      if (a=""){console.log(todo)}
      else if (b="") {console.log(todo)}
      else if (c="") {console.log(todo)}
      else if (d="") {console.log(todo)}
      else if (e="") {console.log(todo)}
      else if (g="") {console.log(todo)}
};
}
